- hosts: all
  gather_facts: no
  remote_user: ustudy
  become: true
  become_method: sudo
  ignore_unreachable: yes
  tasks:
- name: Restart sshd service
  service:
    name: sshd
    state: restarted
